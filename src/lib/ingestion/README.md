# Ingestion (Piece 2 — not built yet)

Will load `data/product-brief.md` and `data/supplier-profiles/*.md` as raw text (format-agnostic — no Markdown-structure assumptions, per `CLAUDE.md`) and chunk each document into citable sections.
