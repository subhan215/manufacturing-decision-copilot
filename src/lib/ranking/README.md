# Ranking engine (Piece 5 — not built yet)

Scores eligible suppliers against weighted priorities (cost/lead-time/quality/sustainability). Weights are adjustable at request time to support the sensitivity-analysis requirement. Pure TypeScript scoring logic, no LLM call needed here.
