# Baseline comparator (Piece 6 — not built yet)

Deliberately simple, non-AI baseline (naive keyword match / hard-coded threshold rule) to compare against the AI eligibility/ranking output, per the rubric's "measurable improvement over a baseline" requirement.
